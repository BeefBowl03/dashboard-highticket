{
  "name": "dashboard-unified",
  "version": "1.0.0",
  "description": "Unified Dashboard with Policy Generator and Domain Generator",
  "private": true,
  "scripts": {
    "dev": "cd dashboard-app && npm start",
    "build": "cd dashboard-app && npm run build",
    "start": "cd dashboard-app && npm start",
    "install": "cd dashboard-app && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}